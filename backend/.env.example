NODE_ENV=development
PORT=4000

DB_HOST=127.0.0.1
DB_USER=root
DB_PASS=secret
DB_NAME=mrms

JWT_SECRET=replace-with-strong-secret
JWT_REFRESH_SECRET=replace-with-strong-refresh-secret

SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=smtp-user
SMTP_PASS=smtp-pass
SMTP_FROM=MRMS <no-reply@example.com>
