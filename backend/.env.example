# Database
DB_HOST=localhost
DB_USER=root
DB_PASS=secret
DB_NAME=mrms

# JWT
JWT_SECRET=change_me
JWT_REFRESH_SECRET=change_me_refresh

# SMTP
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=smtp_user
SMTP_PASS=smtp_pass
SMTP_FROM=mrms@example.com

# App
NODE_ENV=development
PORT=4000
# Optional: initial KMC contact email
KMC_CONTACT_EMAIL=kmc@example.com
